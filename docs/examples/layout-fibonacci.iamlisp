#!iamlisp layout-v1
; Any indentation increase opens one level. Inline parentheses remain available.

defun fib (n)
  cond
       (<= n 1) n
       else
          + (fib (- n 1)) (fib (- n 2))

fib 10
