; Prints numbers from 100 to 0

(loop (x 100)
      (print x)
      (cond (> x 0) (recur (- x 1))))

; Fibonacci

(defun fib (n)
  (loop (x 0 y 1 i n)
    (def fibn (+ x y))
    (cond (zero? i) fibn (recur y fibn (dec i)))))
