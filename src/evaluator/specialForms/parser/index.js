import withMeta from "./with-meta";
import getMeta from "./get-meta";
import quote from "./quote";

const parserForms = { "with-meta": withMeta, "get-meta": getMeta, quote };

export default parserForms;
