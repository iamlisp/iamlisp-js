const parseSymbol = (code, offset) => {};

const parseList = (code, offset) => {};

const parseMap = (code, offset) => {};

const parseString = (code, offset) => {};

const parseQuotedString = (code, offset) => {};

const parseInlineComment = (code, offset) => {};
